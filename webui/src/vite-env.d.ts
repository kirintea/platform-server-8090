/// <reference types="vite/client" />
// NOTE: `vite-plugin-svgr` is intentionally NOT a dependency and no `.svg` is imported
// as a component, so the svgr client reference was removed to fix `tsc -b` TS2688.
